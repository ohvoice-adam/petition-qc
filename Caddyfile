{
    # Switch to staging CA to avoid rate limits during testing.
    # Remove this block once the rate limit expires (2026-02-26 06:13 UTC).
    acme_ca https://acme-staging-v02.api.letsencrypt.org/directory
}

{$DOMAIN} {
    request_body {
        max_size 1GB
    }
    reverse_proxy web:8000
}
